Der Bezeichner %[1]s wird verwendet bzw. deklariert.
Es wurde aber bisher kein Typ mit dem Namen %[1]s definiert.
Erstelle daher eine passende Definition im gleichen Kontext.

Beispiele:

Typ %[1]s = Text

// oder

Daten %[1]s {
    //...
}